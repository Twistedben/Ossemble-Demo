// Triggered when an city admin clicks Acknowledge on Complaint page, created through AJAX. Rendered from controller.
// Below - Adds JS effects for creation of a city reply, Flash alerts, and animation. 

  $('#complaint_reply_div').delay(1000).slideDown("slow", "swing");
  $('#acknowledge_form').fadeIn("slow", "swing");
  $('#acknowledge_form').delay(250).effect('highlight', 3000, 2000);
  