<!--  Partial for fields for creating a MapPin record off a Workplace Suggestion to provide latitude and longitude, 
    category, radius and id when a shape is drawn on the map. Rendered from Workplace_suggestions => new.html.erb. 
    Values are provided from _new_map_pin_map.html.erb partial -->

    <%= f.hidden_field :lat_lng %>
    <%= f.hidden_field :category %>
    <%= f.hidden_field :radius %>