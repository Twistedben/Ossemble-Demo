# Main javascript for filter panel panel. Allows button dropdown for desktop. And applies filter panel resize fix when loading on desktop small window to big.

#$(document).ready ->
#  $('li.dropdown a').on 'click', (event) ->
#    $(this).parent().toggleClass 'open'
#    return
#  $(window).resize ->
#    if $(window).width() > 999
#      if $('body').hasClass('sidr-open')
#        $('#simple-menu').click()
#      else
#    else
#      if $('body').hasClass('sidr-open')
#        $('#simple-menu').click()
#      else
#    return
#  return